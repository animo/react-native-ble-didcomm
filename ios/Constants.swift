enum Constants {
    /*  The number of bytes we adjust for in the chunking algorithm to account for the data header 
    (3 bytes) + 9 extra bytes to be sure = 12 bytes */
    static let NUMBER_OF_BYTES_FOR_DATA_HEADER: Int = 12
}
